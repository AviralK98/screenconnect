# Architecture

Full architecture documentation has moved to the Obsidian vault:

**`docs/Architecture/Overview.md`** — directory structure, layer responsibilities, Qt↔asyncio bridge, feature/handler pattern  
**`docs/Architecture/Protocol.md`** — complete message protocol reference (JSON and binary lanes)  
**`docs/Architecture/Config Reference.md`** — all config fields with defaults and env var overrides  

Open the `docs/` folder in [Obsidian](https://obsidian.md) for the best reading experience.
